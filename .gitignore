node_modules/
dist/
.wrangler/

# Secrets — never commit
.dev.vars
.env
.env.*

# Vendored ffmpeg core — generated by `npm run vendor:ffmpeg` on every install,
# so never committed. `vendor/` is the pristine copy; `public/vendor/` holds the
# split parts that ship as static assets.
vendor/
public/vendor/

.DS_Store
*.log
